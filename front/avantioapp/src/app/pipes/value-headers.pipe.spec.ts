import { ValueHeadersPipe } from './value-headers.pipe';

describe('ValueHeadersPipe', () => {
  it('create an instance', () => {
    const pipe = new ValueHeadersPipe();
    expect(pipe).toBeTruthy();
  });
});
